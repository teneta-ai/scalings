{
  "name": "scalings",
  "version": "1.0.0",
  "type": "module",
  "description": "Autoscaling simulator for Kubernetes HPA, AWS ASG, and GCP MIG",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/tests/*.test.js"
  },
  "keywords": ["autoscaling", "kubernetes", "hpa", "simulator", "capacity-planning"],
  "homepage": "https://scalings.xyz/",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^5.8.3"
  }
}
